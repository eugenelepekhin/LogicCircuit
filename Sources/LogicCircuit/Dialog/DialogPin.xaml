<Window x:Class="LogicCircuit.DialogPin"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:lc="clr-namespace:LogicCircuit"
	xmlns:properties="clr-namespace:LogicCircuit.Properties"
	WindowStartupLocation="CenterOwner"
	Title="{Binding Source={x:Static properties:Resources.NamePin}, Mode=OneTime}"
	Style="{DynamicResource DialogWindow}"
	MinWidth="320" MinHeight="320"
	FocusManager.FocusedElement="{Binding ElementName=name}"
>
	<Window.Resources>
		<Style TargetType="{x:Type TextBox}">
			<Setter Property="Margin" Value="3,3,8,3"/>
		</Style>

		<Style TargetType="{x:Type Label}">
			<Setter Property="Margin" Value="3"/>
			<Setter Property="HorizontalContentAlignment" Value="Right"/>
		</Style>

		<Style TargetType="{x:Type ComboBox}">
			<Setter Property="Margin" Value="3"/>
			<Setter Property="MinWidth" Value="65"/>
			<Setter Property="HorizontalAlignment" Value="Left"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
		</Style>
	</Window.Resources>

	<Grid Margin="3">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<Label Grid.Row="0" Grid.Column="0" Content="{Binding Source={x:Static properties:Resources.TitleType}, Mode=OneTime}"/>
		<TextBlock Grid.Row="0" Grid.Column="1" Name="type" VerticalAlignment="Center" Margin="3"/>

		<Label Grid.Row="1" Grid.Column="0" Content="{Binding Source={x:Static properties:Resources.TitleName}, Mode=OneTime}" Target="{Binding ElementName=name}"/>
		<TextBox Grid.Row="1" Grid.Column="1" Name="name"/>

		<Label Grid.Row="2" Grid.Column="0" Content="{Binding Source={x:Static properties:Resources.TitleNotation}, Mode=OneTime}" Target="{Binding ElementName=notation}"/>
		<TextBox Grid.Row="2" Grid.Column="1" Name="notation" ToolTip="{x:Static properties:Resources.ToolTipPinNotation}"/>

		<Label Grid.Row="3" Grid.Column="0" Content="{Binding Source={x:Static properties:Resources.TitleSide}, Mode=OneTime}" Target="{Binding ElementName=side}"/>
		<ComboBox Grid.Row="3" Grid.Column="1" Name="side" ToolTip="{x:Static properties:Resources.ToolTipPinSide}"/>

		<Label Grid.Row="4" Grid.Column="0" Content="{Binding Source={x:Static properties:Resources.TitleBitWidth}, Mode=OneTime}" Target="{Binding ElementName=bitWidth}"/>
		<ComboBox Grid.Row="4" Grid.Column="1" Name="bitWidth" ToolTip="{x:Static properties:Resources.ToolTipPinBitWidth}"/>

		<CheckBox Grid.Row="5" Grid.Column="1" Name="inverted" Content="{Binding Source={x:Static properties:Resources.TitleInverted}, Mode=OneTime}" IsThreeState="False"/>

		<Label Grid.Row="6" Grid.Column="0" Content="{Binding Source={x:Static properties:Resources.TitleDescription}, Mode=OneTime}" Target="{Binding ElementName=note}"/>
		<TextBox Grid.Row="6" Grid.Column="1" Name="note" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
			AcceptsReturn="True" AcceptsTab="True" SpellCheck.IsEnabled="True"
		/>

		<StackPanel Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
			<Button Content="{Binding Source={x:Static properties:Resources.TitleOK}, Mode=OneTime}" IsDefault="True" Click="ButtonOkClick"/>
			<Button Content="{Binding Source={x:Static properties:Resources.TitleCancel}, Mode=OneTime}" IsCancel="True"/>
		</StackPanel>
	</Grid>
</Window>
