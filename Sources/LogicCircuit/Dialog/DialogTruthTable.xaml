<Window x:Class="LogicCircuit.DialogTruthTable"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:lc="clr-namespace:LogicCircuit"
	WindowStartupLocation="CenterOwner"
	Title="{Binding Source={x:Static lc:Resources.CommandTruthTable}, Mode=OneTime}"
	Style="{DynamicResource DialogWindow}"
>
	<!--FocusManager.FocusedElement="{Binding ElementName=name}"-->

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<Grid Grid.Row="0" Margin="3">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>
			<Label Grid.Column="0" Content="_Filter:" Target="{Binding ElementName=filter}"/>
			<TextBox Grid.Column="1" Name="filter" VerticalAlignment="Center"/>
			<Button Grid.Column="2" Content="_Apply" Click="ButtonApplyClick"/>
		</Grid>

		<DataGrid Grid.Row="1" Name="dataGrid" AutoGenerateColumns="False" IsReadOnly="True" Margin="3"
			VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Standard"
			ItemsSource="{Binding Path=TruthTable}"
		>
			<DataGrid.CellStyle>
				<Style TargetType="DataGridCell">
					<Setter Property="HorizontalAlignment" Value="Right"/>
				</Style>
			</DataGrid.CellStyle>
		</DataGrid>

		<Button Grid.Row="2" Content="{Binding Source={x:Static lc:Resources.TitleOK}, Mode=OneTime}" IsDefault="True" IsCancel="True" HorizontalAlignment="Right"/>
	</Grid>
</Window>
